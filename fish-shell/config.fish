# Add Sublime Text 2 launcher.
set -x PATH "/Applications/Sublime Text 2.app/Contents/SharedSupport/bin" $PATH

# Link the git bin.
set -x PATH "/usr/local/git/bin" $PATH

# Link the mongodb bin
set -x PATH "/usr/local/mongodb/bin" $PATH
